apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - n8n
  - technitium
  - vault
  - external-secrets
  - milvus-operator
  - monitoring
  - rabbitmq-operator
  - rabbitmq
  - milvus
  - ollama
  - cloudnative-pg
  - tailscale-operator
  - forgejo-runner
  - cert-manager-kustomization.yaml   # isolated Flux Kustomization -> ./apps/cert-manager
  # NOTE: Marga is staged but not wired here yet. When finishing the B-R1-01
  # deploy, add:  - marga-kustomization.yaml  (its own isolated Flux Kustomization
  # -> ./apps/marga; do NOT add a bare `- marga`).
